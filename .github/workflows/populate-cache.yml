name: 'codemention: populate cache'

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  populate_cache:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./populate-cache
